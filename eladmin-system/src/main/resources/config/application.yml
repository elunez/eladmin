server:
  port: 8000

spring:
  profiles:
    active: dev
  jackson:
    time-zone: GMT+8
  data:
    redis:
      repositories:
        enabled: false

  #配置 Jpa
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: true

  redis:
    #数据库索引
    database: 0
    host: 127.0.0.1
    port: 6379
    password:
    jedis:
      pool:
        #最大连接数
        max-active: 100
        #最大阻塞等待时间(负数表示没限制)
        max-wait: 2000
        #最大空闲
        max-idle: 500
        #最小空闲
        min-idle: 8
    #连接超时时间
    timeout: 5000

#  servlet:
#    multipart:
#      # 上传一个文件最大值,默认1Mb
#      max-file-size: 5KB
#      # 上传多个文件最大值,50Mb,默认10Mb
#      max-request-size: 5KB

#七牛云
qiniu:
  # 文件大小 /M
  max-size: 1

#验证码有效时间/分钟
code:
  expiration: 5
